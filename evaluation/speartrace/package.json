{
    "name": "speartrace",
    "version": "0.0.1",
    "description": "Frida agent for logging SSL traffic on Android",
    "private": true,
    "main": "agent/socket_trace.ts",
    "scripts": {
        "prepare": "npm run build",
        "build": "frida-compile agent/socket_trace.ts -o _socket_trace.js",
        "watch": "frida-compile agent/socket_trace.ts -o _socket_trace.js -w"
    },
    "devDependencies": {
        "@types/frida-gum": "^16.0.0",
        "@types/node": "^14.0.14",
        "frida-compile": "^9.3.0"
    }
}
